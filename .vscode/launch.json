{
    "version": "0.1.0",
    "configurations": [
        {
            "name": "Launch Package",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${workspaceFolder}/cmd/hyml/hyml.go",
            "cwd": "${workspaceFolder}",
            "args": ["examples/tower.hyml"]
        }
    ]
}